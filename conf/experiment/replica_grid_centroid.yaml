# @package _global_
defaults:
  - base
  - /dataset: Replica
  - /segmentation: GridMobileSAM  # Keep top level for easier overrides
  - /ft_extraction: CLIP # Keep top level for easier overrides
  - _self_

dataset:
  base_path: /home/sacha/Downloads/Replica
  sequence_stride: 5
  resized_width: 600
  resized_height: 340

perception:
  crop_bg_color: 255

