# @package _global_
defaults:
  - replica_grid_centroid
  - override /segmentation: YoloMobileSAM
  - _self_

mapping:
  geometric_sim_thresh: 4.
  semantic_sim_thresh: .75
  min_segments: 3
  filter_min_every: -1
  denoise_pcd_every: 10
  geometry_mode: chamferdist_min
  object_factory:
    n_sample_pcd: 80

perception:
  bg_sim_thresh: 0.21

segmentation:
  yolo_device: cuda