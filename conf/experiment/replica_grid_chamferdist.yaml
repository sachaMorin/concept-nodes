# @package _global_
defaults:
  - replica_grid_centroid
  - _self_

name: replica_grid_chamferdist

mapping:
  geometric_sim_thresh: 7.
  semantic_sim_thresh: .75
  filter_min_every: -1
  denoise_pcd_every: 10
  geometry_mode: chamferdist_min
  object_factory:
    n_sample_pcd: 50
